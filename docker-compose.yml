version: '3'

networks:
    laravel:
        name: laravel

services:
    site:
        image: nginx:1-alpine
        container_name: nginx
        restart: unless-stopped
        ports:
            - "${NGINX_PORT:-8081}:80"
        volumes:
            - ./:/var/www/html
            - ./docker/nginx/nginx.conf:/etc/nginx/nginx.conf
            - ./docker/nginx/general.conf:/etc/nginx/general.conf
            - ./docker/nginx/php_fastcgi.conf:/etc/nginx/php_fastcgi.conf
            - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
        depends_on:
            - mysql
            - php
            - redis
        networks:
            - laravel

    mysql:
        image: mysql:8
        container_name: mysql
        restart: unless-stopped
        tty: true
        ports:
            - "3307:3306"
        environment:
            MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
            MYSQL_ROOT_HOST: "%"
            MYSQL_ROOT_PASSWORD: "${DB_PASSWORD}"
            MYSQL_DATABASE: "${DB_DATABASE}"
            MYSQL_USER: "${DB_USERNAME}"
            MYSQL_PASSWORD: "${DB_PASSWORD}"
        entrypoint:
            sh -c "
            echo 'CREATE DATABASE IF NOT EXISTS ${DB_DATABASE}; CREATE DATABASE IF NOT EXISTS ${DB_DATABASE_TEST}' > /docker-entrypoint-initdb.d/init.sql;
            /usr/local/bin/docker-entrypoint.sh --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
            "
        volumes:
            - "${DB_VOLUME:-~/.mysql8/}:/var/lib/mysql"
        networks:
            - laravel

    php:
        # image: devmoath/php-74-laravel-dev
        image: devmoath/php-8-laravel-dev
        container_name: php
        restart: unless-stopped
        volumes:
            - ./:/var/www/html
            - "~/.config/psysh/psysh_history:/root/.config/psysh/psysh_history"
            - "~/.composer:/tmp"
        networks:
            - laravel

    redis:
        image: redis:6-alpine
        container_name: redis
        restart: unless-stopped
        ports:
            - "${REDIS_PORT:-6379}:6379"
        networks:
            - laravel

    horizon:
        # image: devmoath/php-74-laravel-dev
        image: devmoath/php-8-laravel-dev
        container_name: horizon
        restart: unless-stopped
        working_dir: /var/www/html
        entrypoint: bash -c "while :; do timeout 60s php artisan horizon --ansi; done"
        volumes:
            - ./:/var/www/html
        networks:
            - laravel
